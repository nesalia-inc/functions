import { Command } from "../functions/types";
import { Event } from "../events/types";

export type APIConfig = {
  context: undefined;
  commands: Command[];
  events: Event[];
};

export type API = undefined;
